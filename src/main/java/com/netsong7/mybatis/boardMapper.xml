<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mybatis.boardMapper">
	<!-- 전체 글보기 -->
	<select id="getList" resultType="BoardDto">
		select * from tblSpringBoard
		order by b_seq desc
	</select>
	
	<!-- 글쓰기 -->
	<insert id="write" parameterType="BoardDto">
		insert into tblSpringBoard(b_title, b_content, b_writer, 
											b_regdate, b_hitcount, b_password) 
		values(#{b_title},#{b_content},#{b_writer},now(), 0,#{b_password})
	</insert>
	
	<!-- 특정 글보기 -->
	<select id="findBySeq" parameterType="int" resultType="BoardDto">
		select * from tblSpringBoard where b_seq=#{b_seq}
	</select>
</mapper>






